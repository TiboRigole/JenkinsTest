
pipeline {
    agent any
    
    options { skipDefaultCheckout() }

    stages {        
        stage('build') {
            steps {
               echo 'Hello World'
            }
        }
    }
}